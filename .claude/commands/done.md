Stage all changes, commit with a short message describing what was updated, and push to origin main.

Steps:
1. Run `git status` to see what changed
2. Run `git diff --stat` to summarize the changes
3. Stage everything with `git add -A`
4. Write a concise commit message based on what actually changed — name the files or sections edited, not generic phrases like "update files"
5. Commit and push to origin main
6. Confirm success with a one-line summary of what was committed
